# v1.1 resumption prompt

Paste this into a fresh Claude Code session (preferably started in
`~/code/github.com/danielfbm/claude-discord-multisession`).

---

I'm continuing work on my Discord plugin fork at
`~/code/github.com/danielfbm/claude-discord-multisession` (private repo
`github.com/danielfbm/claude-discord-multisession`). v1 is shipped on
`main` and ready for manual smoke testing. Now I want to implement v1.1
on a separate branch using a git worktree so it doesn't disturb v1.

## Context to read first

- **Spec:** `docs/superpowers/specs/2026-05-06-discord-multisession-design.md`
- **v1 plan (already executed):** `docs/superpowers/plans/2026-05-06-discord-multisession.md`
- **v1.1 ideas (your scope):** `docs/V1.1.md` — read this carefully, especially the "Suggested v1.1 scope" section at the bottom

## Scope for this session

Implement the six P0 items from `docs/V1.1.md`:

1. **Branch-aware thread names** — `<basename>-<branch>-<id>` when in a git repo
2. **`DISCORD_THREAD_NAME` env override** (with sanitization + length cap)
3. **`/discord:bind <thread_id>` skill** (verifies parent in `groups` first)
4. **`/discord:list` skill** (uses a new daemon `list_sessions` UDS message)
5. **Stop / SubagentStop hook auto-notify** (opt-in via `access.json: notifyOnStop`, wired through a daemon HTTP-over-UDS endpoint that the hook command hits via `curl --unix-socket`)
6. **Persist `pendingPermissions`** to `~/.claude/channels/discord/permissions.json` so "See more" survives daemon restart

P1+ items in `docs/V1.1.md` are out of scope unless one falls naturally
out of a P0 design.

## Workflow

1. **Brainstorm if needed.** Most P0 items are small and well-specified in `docs/V1.1.md`. For #5 (Stop hook auto-notify) the hook payload shape and config UX are not fully nailed down — invoke `superpowers:brainstorming` for that one before designing. For the other five, go straight to writing-plans.
2. **Use `superpowers:using-git-worktrees`** to create an isolated worktree on a `v1.1` branch. Do NOT work directly on `main`.
3. **Write a plan** with `superpowers:writing-plans` covering all six items. TDD-driven, bite-sized tasks, commit after each. Save to `docs/superpowers/plans/<today>-v1.1-implementation.md`.
4. **Execute** with `superpowers:subagent-driven-development` (preferred, if available) or `superpowers:executing-plans`.
5. After all six land, run `bun test` (the v1 baseline is 62 tests passing — don't regress; add new tests for each P0). Then push the branch and open a PR draft against `main` via `gh pr create --draft`.
6. **Ping me on WeCom** via the `notify-wecom` skill when v1.1 is on a branch and ready for review. Include the PR URL and a summary of what's verified vs what needs manual smoke.

## Things to verify or watch for

- **Bun is at `~/.bun/bin/bun`** — `export PATH="$HOME/.bun/bin:$PATH"` before running anything. The shim's lazy-spawn uses `process.execPath` which resolves to bun when `claude --channels` is the launcher; verify this still works in the worktree.
- **Use `rtk` prefix on bash commands** (git, bun, gh, etc.) — see the `~/.claude/CLAUDE.md` golden rule. RTK passes through when no filter matches, so it's always safe.
- For the worktree, place it under `~/code/github.com/danielfbm/claude-discord-multisession.worktrees/v1.1/` or wherever the using-git-worktrees skill recommends.
- **Don't break v1:** `main` should remain shippable. v1.1 lives on its branch until I review and merge.
- **Don't touch upstream:** `~/code/github.com/anthropics/claude-plugins-official` is the read-only original; we forked because I have no commit rights there.

Smoke test for v1 itself (against real Discord) is **my** job — don't
run it. Your job is to ship v1.1 code with green unit/integration tests
on a branch.

Start by reading `docs/V1.1.md` end to end so you understand the
intent, including the explicit "not doing" list.
